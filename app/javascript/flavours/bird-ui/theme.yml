# The bird-ui flavour layers the Mastodon Bird UI stylesheets over the
# stock frontend. The Bird UI look is pure CSS (see skins/bird-ui/), so
# pack_directory points at the shared entrypoint tree, exactly like
# mastodon-ui, and no app code is duplicated.
pack_directory: app/javascript/entrypoints
